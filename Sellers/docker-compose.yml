services:
  sellers-frontend:
    build: .
    container_name: hands_and_hope_sellers_frontend
    ports:
      - "3001:3000"
    environment:
      - REACT_APP_BACKEND_URL=http://host.docker.internal:5000
      - NODE_ENV=production
    volumes:
      - .:/app
      - /app/node_modules